spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/learning
    username: learning
    password: learning
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
